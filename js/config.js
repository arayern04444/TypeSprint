/* =====================================================================
   Supabase project config.

   These values are meant to be public — SUPABASE_ANON_KEY is Supabase's
   "publishable" client key. It's safe to ship in browser code and has
   no privileged access on its own; real security comes from the Row
   Level Security policies defined on each table in Postgres (see the
   sql/ folder), not from hiding this key.
===================================================================== */
export const SUPABASE_URL = 'https://anyoqgvhjajxpjrmvleu.supabase.co';
export const SUPABASE_ANON_KEY = 'sb_publishable_MCnu0fN6pji701ay95cQjQ_hMmdBTPT';
